simple python network sniffer using scapy

# requirements

python 3.10+
npcap (windows)

```pip install scapy```
```python main.py```

what it does:
captures ip traffic
counts traffic per ip
hides private networks
shows top active ips in terminal

notes
install npcap if no packets appear
